<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme.
         Pure Compose app — visual theming is handled by ui/theme/Theme.kt.
         AppCompat.DayNight.NoActionBar is available transitively via activity-compose. -->
    <style name="Theme.Swipy" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">false</item>
    </style>
</resources>